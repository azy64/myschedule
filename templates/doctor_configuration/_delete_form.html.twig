<form method="post" action="{{ path('app_doctor_configuration_delete', {'id': doctor_configuration.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ doctor_configuration.id) }}">
    <button class="btn tunaweza-bg-red text-white hand-over1"><i class="bi bi-trash3-fill"></i></button>
</form>
